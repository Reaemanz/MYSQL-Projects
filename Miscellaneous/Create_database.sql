-- CREATING A DATABASE

CREATE DATABASE Emmanuel;

USE emmanuel;

CREATE table Salary
  (Name_id     INT         PRIMARY KEY    auto_increment,
   VENDOR_id   int         NOT NULL,
   City        varchar(50) not null);
